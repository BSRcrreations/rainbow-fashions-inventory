# Copy to /opt/rainbow-fashions-prod/shared/backup-offsite.env and chmod 600.
# This file must remain outside Git and outside release directories.
RESTIC_REPOSITORY=s3:https://OBJECT_STORAGE_ENDPOINT/BUCKET/rainbow-fashions
RESTIC_PASSWORD=CHANGE_ME
AWS_ACCESS_KEY_ID=CHANGE_ME
AWS_SECRET_ACCESS_KEY=CHANGE_ME
AWS_DEFAULT_REGION=CHANGE_ME

# Set to YES only for an approved, reviewed destructive offsite prune.
OFFSITE_RETENTION_APPROVED=NO
