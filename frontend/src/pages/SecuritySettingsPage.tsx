import { useEffect, useState, type FormEvent } from "react";
import { DatabaseBackup, ShieldCheck } from "lucide-react";
import { api } from "../api/client";
import ErrorState from "../components/ErrorState";
import PageHeader from "../components/PageHeader";
import { useAuth } from "../hooks/useAuth";

type SecurityState = { configured: boolean; require_password_for_sale_delete: boolean; require_password_for_purchase_delete: boolean };
type BackupComponent = { component: string; status: string; available: boolean; details: Record<string, unknown> };
type BackupState = { configured: boolean; components: BackupComponent[] };

function label(component: string) { return component.replace(/_/g, " ").replace(/\b\w/g, (letter) => letter.toUpperCase()); }
function detail(component: BackupComponent) {
  const value = component.details.finished_at ?? component.details.checked_at ?? component.details.message;
  return typeof value === "string" ? value : component.available ? "No timestamp reported" : "No status reported yet";
}

export default function SecuritySettingsPage() {
  const { user } = useAuth();
  const [state, setState] = useState<SecurityState | null>(null); const [backup, setBackup] = useState<BackupState | null>(null); const [error, setError] = useState(""); const [currentCredential, setCurrentCredential] = useState(""); const [newPassword, setNewPassword] = useState(""); const [confirmPassword, setConfirmPassword] = useState(""); const [saving, setSaving] = useState(false); const [saved, setSaved] = useState("");
  useEffect(() => { if (user?.role !== "OWNER") return; Promise.all([api.get<SecurityState>("/security/destructive-actions").then(setState), api.get<BackupState>("/security/backup-status").then(setBackup)]).catch((cause) => setError(cause instanceof Error ? cause.message : "Unable to load security settings")); }, [user?.role]);
  async function saveDeletionPassword(event: FormEvent) {
    event.preventDefault(); setError(""); setSaved("");
    if (newPassword !== confirmPassword) { setError("New deletion passwords do not match."); return; }
    setSaving(true);
    try {
      const next = await api.put<SecurityState>("/security/destructive-actions", { current_credential: currentCredential, new_password: newPassword, confirm_password: confirmPassword });
      setState(next); setCurrentCredential(""); setNewPassword(""); setConfirmPassword(""); setSaved("Deletion password saved. It is never shown or returned by the server.");
    } catch (cause) { setError(cause instanceof Error ? cause.message : "Unable to save deletion password"); }
    finally { setSaving(false); }
  }
  if (user?.role !== "OWNER") return <ErrorState message="Only the store owner can manage destructive action security." />;
  return <><PageHeader title="Security" subtitle="Destructive actions and backup health" /><div className="mx-auto grid max-w-2xl gap-6"><section className="ds-surface p-6"><div className="flex items-start gap-3"><span className="grid h-10 w-10 place-items-center rounded-lg bg-primary-100 text-primary-800"><ShieldCheck size={21} /></span><div><h2 className="font-semibold">Delete protection</h2><p className="mt-1 text-sm text-muted">Set a separate deletion password for this store. Only its secure Argon2 hash is stored; the password is never shown or returned.</p></div></div>{error ? <div className="mt-5"><ErrorState message={error} /></div> : null}{saved ? <p role="status" className="mt-5 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-800">{saved}</p> : null}<dl className="mt-6 divide-y divide-border text-sm"><div className="flex justify-between py-3"><dt>Delete sales</dt><dd className="font-semibold">{state?.require_password_for_sale_delete ? "Enabled" : "Disabled"}</dd></div><div className="flex justify-between py-3"><dt>Delete purchases</dt><dd className="font-semibold">{state?.require_password_for_purchase_delete ? "Enabled" : "Disabled"}</dd></div><div className="flex justify-between py-3"><dt>Deletion password</dt><dd className="font-semibold">{state?.configured ? "Configured" : "Not configured"}</dd></div></dl><form className="mt-6 border-t border-border pt-6" onSubmit={(event) => void saveDeletionPassword(event)}><h3 className="font-semibold">{state?.configured ? "Change deletion password" : "Set deletion password"}</h3><p className="mt-1 text-sm text-muted">Confirm your current Owner password before setting a new deletion password.</p><div className="mt-4 grid gap-4"><label className="field-label">Current Owner password<input className="field-input mt-1" type="password" autoComplete="current-password" value={currentCredential} onChange={(event) => setCurrentCredential(event.target.value)} disabled={saving} required /></label><label className="field-label">New deletion password<input className="field-input mt-1" type="password" autoComplete="new-password" minLength={8} value={newPassword} onChange={(event) => setNewPassword(event.target.value)} disabled={saving} required /></label><label className="field-label">Confirm deletion password<input className="field-input mt-1" type="password" autoComplete="new-password" minLength={8} value={confirmPassword} onChange={(event) => setConfirmPassword(event.target.value)} disabled={saving} required /></label></div><div className="mt-5 flex justify-end"><button type="submit" className="focus-ring rounded-md bg-primary-700 px-4 py-2 text-sm font-semibold text-white hover:bg-primary-800 disabled:cursor-not-allowed disabled:opacity-60" disabled={saving || !currentCredential || newPassword.length < 8 || !confirmPassword}>{saving ? "Saving" : state?.configured ? "Change deletion password" : "Set deletion password"}</button></div></form></section><section className="ds-surface p-6"><div className="flex items-start gap-3"><span className="grid h-10 w-10 place-items-center rounded-lg bg-primary-100 text-primary-800"><DatabaseBackup size={21} /></span><div><h2 className="font-semibold">Backup health</h2><p className="mt-1 text-sm text-muted">Read-only status from the server. Backup contents and credentials are never exposed here.</p></div></div>{backup?.configured === false ? <p className="mt-5 text-sm text-amber-700">Backup status mount is not configured.</p> : <dl className="mt-5 divide-y divide-border text-sm">{backup?.components.map((component) => <div className="flex items-start justify-between gap-4 py-3" key={component.component}><dt><div>{label(component.component)}</div><div className="mt-1 text-xs text-muted">{detail(component)}</div></dt><dd className={`font-semibold ${component.status === "success" ? "text-emerald-700" : component.status === "warning" ? "text-amber-700" : component.status === "critical" || component.status === "failed" ? "text-rose-700" : "text-slate-500"}`}>{component.status}</dd></div>)}</dl>}</section></div></>;
}
