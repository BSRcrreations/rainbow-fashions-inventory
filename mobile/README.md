# Rainbow Fashions Android App

React Native Expo scaffold for Android-only access to the same FastAPI backend used by the web app.

Run:

```bash
cd mobile
npm install
npm run android
```

The API base URL is configured in `app.json` under `expo.extra.apiBaseUrl`.
